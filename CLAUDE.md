# CLAUDE.md

This repository's contributor and agent guidance lives in **[AGENTS.md](./AGENTS.md)**.

Read it before making changes. It is the single source of truth for architecture, the
AppleScript security boundary, branch/PR procedure, commit conventions, versioning, and the
shipped product skill. This file exists only so Claude/Claude Code pick up the same guidance —
do not duplicate content here; update `AGENTS.md` instead.
